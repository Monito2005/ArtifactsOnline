spring:
  datasource:
    url: jdbc:h2:mem:hogwarts
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
api:
  endpoint:
    base-url: /api/v1
jwt:
  secret-key: change-this-secret-key-change-this-secret-key
  expiration-seconds: 3600
